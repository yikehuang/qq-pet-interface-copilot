# Third-party notices

`QQPetProtocolHost` links to [MiraiGo](https://github.com/Mrs4s/MiraiGo), pinned
in `go.sum` through module version
`v0.0.0-20240226124653-54bdd873e3fe`. MiraiGo is distributed under the
GNU Affero General Public License v3.0.

The host does not contain QQ, an Android APK, qsign native libraries, account
passwords, cookies, or user session files. A compatible local Android signing
runtime is an external prerequisite and must remain bound to the local machine.

Source code for this exact host build is in the `protocol-host` directory of
the same project release/tag.
